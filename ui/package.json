{
  "name": "cabas-ui",
  "version": "0.1.0-dev",
  "private": true,
  "type": "module",
  "description": "cabas — the PWA front end. Renders view-models pushed by cabas-app and emits intents (CONSTITUTION Rule 9).",
  "license": "MIT OR Apache-2.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json --fail-on-warnings"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "7.3.0",
    "svelte": "5.56.8",
    "svelte-check": "4.7.5",
    "typescript": "5.9.3",
    "vite": "8.2.1"
  }
}
